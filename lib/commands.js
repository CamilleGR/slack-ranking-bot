
const regex_stat = /^cheo\,?\ montre\ (moi|nous|leur|lui)\ les\ stats\ de\ l\'(\w*)\ ([\w-+]*)$/im
const regex_ping = /^((c|C)heo\,?\s?((es tu)|(tu es))?\s?là\s?\?\s?)|(cheo\s?\?)$/

module.exports =  {
                    regex_stat: regex_stat,
                    regex_ping: regex_ping
                  }
